[
  {
    "file": "src/lib/error-kind.ts",
    "description": "⚠️⚠️ una scheda rimasta su una versione precedente non viene più riconosciuta: l'utente legge «qualcosa è andato storto» su una pagina che basterebbe ricaricare",
    "find": "  /module factory is not available|chunkloaderror|loading chunk .+? failed|failed to fetch dynamically imported module|importing a module script failed/i;",
    "replace": "  /chunkloaderror/i;"
  },
  {
    "file": "src/lib/error-kind.ts",
    "description": "⚠️⚠️ ogni errore viene scambiato per una versione vecchia: un rifiuto di permessi sparisce dietro un ricaricamento continuo",
    "find": "  if (STALE_BUILD.test(m)) return \"stale\";",
    "replace": "  return \"stale\";"
  },
  {
    "file": "src/lib/error-kind.ts",
    "description": "⚠️ sparisce il freno al ricaricamento: se ricaricare non risolve, la scheda entra in un ciclo infinito",
    "find": "    if (now - last < RELOAD_WINDOW_MS) return false;",
    "replace": ""
  },
  {
    "file": "src/lib/error-kind.ts",
    "description": "⚠️ una scheda che non può ricordare di averci già provato ricarica comunque, e lampeggia senza fine",
    "find": "  if (!store) return false;",
    "replace": "  if (!store) return true;"
  }
]
