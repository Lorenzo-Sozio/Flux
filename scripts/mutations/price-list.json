[
  {
    "file": "src/lib/price-list.ts",
    "description": "⚠️⚠️ il prezzo scritto a mano nel listino viene ignorato: il cliente paga la percentuale su un prodotto che era stato concordato",
    "find": "  const override = rules.overrides[productId];\n  if (override !== undefined && Number.isFinite(override)) {",
    "replace": "  const override = undefined as number | undefined;\n  if (override !== undefined && Number.isFinite(override)) {"
  },
  {
    "file": "src/lib/price-list.ts",
    "description": "⚠️⚠️ un prodotto regalato (prezzo 0) torna al prezzo di catalogo: si fattura ciò che era stato dato",
    "find": "    return { price: round2(Math.max(0, override)), source: \"override\" };",
    "replace": "    if (override > 0) return { price: round2(override), source: \"override\" };"
  },
  {
    "file": "src/lib/price-list.ts",
    "description": "⚠️⚠️ il segno si rovescia: uno sconto del 10% diventa una maggiorazione del 10%",
    "find": "  return { price: round2(Math.max(0, base * (1 + percent / 100))), source: \"percent\" };",
    "replace": "  return { price: round2(Math.max(0, base * (1 - percent / 100))), source: \"percent\" };"
  },
  {
    "file": "src/lib/price-list.ts",
    "description": "⚠️ un listino oltre il -100% produce un prezzo negativo: si pagherebbe il cliente per ritirare la merce",
    "find": "  return { price: round2(Math.max(0, base * (1 + percent / 100))), source: \"percent\" };",
    "replace": "  return { price: round2(base * (1 + percent / 100)), source: \"percent\" };"
  },
  {
    "file": "src/lib/price-list.ts",
    "description": "⚠️ il prezzo non viene arrotondato al centesimo: la colonna non lo regge e i totali non tornano con quanto mostrato",
    "find": "function round2(n: number): number {\n  return Math.round((n + Number.EPSILON) * 100) / 100;\n}",
    "replace": "function round2(n: number): number {\n  return n;\n}"
  },
  {
    "file": "src/lib/price-list.ts",
    "description": "un prezzo illeggibile diventa NaN invece di zero, e ogni totale del documento diventa illeggibile",
    "find": "  return Number.isFinite(n) ? n : 0;",
    "replace": "  return n;"
  },
  {
    "file": "src/lib/price-list.ts",
    "description": "senza listino il prodotto non prende più il prezzo di catalogo",
    "find": "  if (!rules) return { price: base, source: \"base\" };",
    "replace": "  if (!rules) return { price: 0, source: \"base\" };"
  },
  {
    "file": "src/lib/price-list.ts",
    "description": "⚠️ la direzione della variazione si perde: lo schermo dice «sconto» anche per una maggiorazione",
    "find": "  return percent < 0 ? { direction: \"off\", percent: Math.abs(percent) } : { direction: \"on\", percent };",
    "replace": "  return { direction: \"off\", percent: Math.abs(percent) };"
  }
]
