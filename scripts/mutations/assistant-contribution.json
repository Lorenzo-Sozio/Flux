[
  {
    "file": "src/actions/assistant-contribution.ts",
    "description": "An order with no recorded provenance is counted as a person's: a number invented for a period the database cannot know",
    "find": "where ${orders.source} is not null and ${orders.source} <> ${SORGENTE_ASSISTENTE}",
    "replace": "where ${orders.source} <> ${SORGENTE_ASSISTENTE}"
  },
  {
    "file": "src/actions/assistant-contribution.ts",
    "description": "Unrecorded orders stop being reported at all: the total is lower than the truth and plausible",
    "find": "      nonRegistrati: sql<number>`count(*) filter (where ${orders.source} is null)::int`,\n",
    "replace": "      nonRegistrati: sql<number>`0::int`,\n"
  },
  {
    "file": "src/actions/assistant-contribution.ts",
    "description": "The period lands on the wrong column: a month's takings move when a row is edited",
    "find": ".where(and(gte(orders.orderDate, da), lte(orders.orderDate, a)));",
    "replace": ".where(and(gte(orders.updatedAt, da), lte(orders.updatedAt, a)));"
  },
  {
    "file": "src/actions/assistant-contribution.ts",
    "description": "The contacts count gains a period: a full address book reads as zero on the first of every month",
    "find": "    .from(contacts);",
    "replace": "    .from(contacts)\n    .where(and(gte(contacts.createdAt, da), lte(contacts.createdAt, a)));"
  },
  {
    "file": "src/actions/assistant-contribution.ts",
    "description": "An empty database returns undefined instead of zero: the screen shows NaN on every new workspace",
    "find": "    contattiDallAssistente: perContatti?.assistente ?? 0,",
    "replace": "    contattiDallAssistente: perContatti?.assistente as number,"
  },
  {
    "file": "src/actions/assistant-contribution.ts",
    "description": "The provenance word is written a second time here instead of shared with whoever writes it",
    "find": "export const SORGENTE_ASSISTENTE = \"assistant\";",
    "replace": "export const SORGENTE_ASSISTENTE = \"assistente\";"
  }
]
