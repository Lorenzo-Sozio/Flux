[
  {
    "file": "src/lib/erasure.ts",
    "description": "⚠️⚠️ il contatto viene anonimizzato PER PRIMO: brucia l'indice, e da quel momento nessuna traccia e' piu' raggiungibile dal recapito — il dato resta e la capacita' di finire il lavoro se ne va",
    "find": "  // ── 1. The lead goes entirely",
    "replace": "  if (person.contactIds.length > 0) {\n    await db.update(contacts).set({ ...CAMPI_DELLA_PERSONA }).where(inArray(contacts.id, person.contactIds)).returning({ id: contacts.id });\n  }\n  // ── 1. The lead goes entirely"
  },
  {
    "file": "src/lib/erasure.ts",
    "description": "la persona si cerca DOPO aver cancellato: gli id dei contatti arrivano vuoti e nessuna traccia viene ripulita",
    "find": "  const person = await findByContactPoint(db, email, digits);\n  const report: ErasureReport",
    "replace": "  const person: ReachablePerson = { leadIds: [], contactIds: [], email, digits };\n  const report: ErasureReport"
  },
  {
    "file": "src/lib/erasure.ts",
    "description": "il diario scritto su quella persona sopravvive all'anonimizzazione: il nome resta nel testo libero",
    "find": "    report.deleted.activity = quanti(\n      await db.delete(activities)",
    "replace": "    report.deleted.activity = 0 * quanti(\n      await db.select(activities)"
  },
  {
    "file": "src/lib/erasure.ts",
    "description": "un contatto viene cancellato invece che anonimizzato: con una trattativa o un ticket il DELETE fallisce per vincolo, e senza fallire distrugge i conti dell'azienda",
    "find": "      await db\n        .update(contacts)\n        .set({ ...CAMPI_DELLA_PERSONA, updatedAt: new Date() })",
    "replace": "      await db\n        .delete(contacts)\n        .set({ ...CAMPI_DELLA_PERSONA, updatedAt: new Date() })"
  },
  {
    "file": "src/lib/erasure.ts",
    "description": "l'email della persona sopravvive all'anonimizzazione del contatto: la riga resta e la persona e' ancora identificabile",
    "find": "  email: null,\n  phone: null,\n  mobile: null,",
    "replace": "  phone: null,\n  mobile: null,"
  },
  {
    "file": "src/lib/erasure.ts",
    "description": "il nome non viene sovrascritto: la colonna e' NOT NULL, quindi la persona resta chiamata com'era",
    "find": "  firstName: ANONIMO,",
    "replace": "  city: null,"
  },
  {
    "file": "src/lib/erasure.ts",
    "description": "le parole della persona restano dentro il ticket: subject e description non vengono toccati",
    "find": "        .set({ subject: ANONIMO, description: null })",
    "replace": "        .set({ description: null })"
  },
  {
    "file": "src/lib/erasure.ts",
    "description": "il testo scritto dalla persona nel ticket_message sopravvive",
    "find": "        .set({ senderEmail: null, senderName: ANONIMO, content: \"\" })",
    "replace": "        .set({ senderName: ANONIMO })"
  },
  {
    "file": "src/lib/erasure.ts",
    "description": "l'indirizzo IP lasciato aprendo un preventivo sopravvive alla cancellazione",
    "find": "      await db\n        .delete(quoteActivities)",
    "replace": "      await db\n        .select(quoteActivities)"
  },
  {
    "file": "src/lib/erasure.ts",
    "description": "la coda di invio sopravvive, con l'indirizzo e il corpo dei messaggi mandati a quella persona",
    "find": "      await db\n        .delete(emailJobs)",
    "replace": "      await db\n        .select(emailJobs)"
  },
  {
    "file": "src/lib/contact-point.ts",
    "description": "un recapito che non e' ne' email ne' telefono viene accettato: un refuso riceve «non trovato», che a chi ha chiesto di sparire dice una cosa diversa",
    "find": "  if (!email && !digits) {\n    throw new Error(\"a contact point must be an email address or a phone number\");\n  }",
    "replace": ""
  },
  {
    "file": "src/lib/contact-point.ts",
    "description": "un recapito vuoto cancella tutti: la richiesta di una persona sola porta via l'anagrafica intera",
    "find": "  if (!raw) throw new Error(\"no contact point to erase\");",
    "replace": ""
  },
  {
    "file": "src/lib/erasure.ts",
    "description": "la lista di soppressione non viene piu' dichiarata come conservata: chi risponde alla persona non sa che quel dato resta",
    "find": "  email_suppression:",
    "replace": "  altro_qualsiasi:"
  },
  {
    "file": "src/lib/erasure.ts",
    "description": "la discussione interna del team non viene piu' elencata: nessuno sa che c'e' una decisione da prendere a mano",
    "find": "  \"deal_comment, dm_message, notification\":",
    "replace": "  \"qualcosa\":"
  },
  {
    "file": "src/lib/erasure.ts",
    "description": "si scrive sul contatto anche quando non ce n'e' nessuno: il rapporto dichiara un lavoro mai avvenuto",
    "find": "  if (contactIds.length > 0) {\n    report.anonymised.contact = quanti(",
    "replace": "  if (true) {\n    report.anonymised.contact = quanti("
  }
]
